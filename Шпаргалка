Создания страницы:
    1] Создать приложение
        1)командой python manage.py startapp Имя_приложения

    2] Добовление в Имя_приложения/templates/Имя_приложения/Страница.html

    3] добовлние в Имя_приложения/urls.py адресс который будет ссылаться на Страница.html
        1) использовать urlpatterns = [path('адресс2', views.mainPage, name='Страница'),]
                             #потом поймёшь почему 2#                     #добовлять.html не надо#

    4] Добовление в viev.py рендера с dict с информацией, которая будет отправляться в Страница.html
                                     #json#

    5] в Имя_сайта/urls.py ипортировать адреса из Имя_приложения/urls.py
 
        1) использовать urlpatterns = [path('адресс1', include('Имя_приложения.urls'))]
  #Страница будет находиться по адрессу:                    #
  #                                    Домен/адресс1/адресс2#

    6] на Страница.html использовать {{ Ключь_переменной_в_dict  }}



Как сделать Гит
    Быстрый старт (для нетерпеливых):
        git init
        git add -A
        git commit -m "first commit"
        git remote add origin https://github.com/you_repository/you_project #подключение к репозиторию
        git push -u origin master
        Все! (Не всё. Были какието проблемы, если чо разберёшься)
    Запушить в гит:
        git status #для проверки
        git add -A #можно без -A но тогдп нада файлы выбирать (а я хз как)
        git commit -m "Текст"
        git push


Как создать модель
    1] Создать в Имя_сайта/apps.py иницилизировать приложение (если ещё не сделал этого)
        1)class УникальноеИмяКласса(AppConfig):
            name = 'имя_приложения'
        2) в Имя_сайта/setting.py в INSTALLED_APPS = [] добавить имя_приложения.apps.УникальноеИмяКласса

        3)python manage.py makemigrations имя_приложения

    2] Создать новый класс модели в имя_приложения/models.py
        class Имя_модели(models.Model):
            имя_параметра = свойства_параметра

    3] В имя_приложения/admin.py импортировать модель и добавить в админ вывод
        1)from .models import имя_модели
                #можно добавить к существующему
        2)admin.site.register(имя_модели)
    